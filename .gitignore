# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.vercel
*.local

# Lockfiles
# O lockfile versionado do projeto é o bun.lock (ver bunfig.toml, que trava
# supply-chain via minimumReleaseAge). Instalar com npm gera o arquivo abaixo
# localmente — ele não deve ser commitado, para não haver dois lockfiles.
package-lock.json

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
